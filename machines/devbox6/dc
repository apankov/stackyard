#!/usr/bin/env bash
# docker compose этой машины. См. комментарий в ./stack.
ROOT_DIR="$( cd -P "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
export ROOT_DIR
exec "$ROOT_DIR/platform/bin/docker-compose.sh" "$@"
