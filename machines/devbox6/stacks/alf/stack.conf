# alf.my-new-site.com — статика прототипа + проксирование в alf-backend.
#
# Контейнер alf-backend этим репозиторием НЕ управляется: он поднимается
# отдельно и просто сидит в той же сети. Для nginx это означает обычный
# upstream, а для нас — что `stack.sh --check` (блок «Upstream'ы включённых
# vhost'ов») здесь единственная проверка того, что адресат вообще жив.
Requires=""
Domains="alf.my-new-site.com"
