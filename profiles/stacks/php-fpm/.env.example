# The php-fpm stack needs no secrets of its own. This file exists for one line
# -- and only if you copied the stack out of the profile into the machine's
# stacks/.
# Php_Stack_Dir=${Platform_Deploy_Dir}/stacks/php-fpm
