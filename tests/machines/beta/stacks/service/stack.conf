# An application with a database in the shared Postgres. The same order keys
# with a different prefix -- the engine must not know the difference.
Requires="pg"
Domains="svc.example.net"
Postgres_DB="${Service_DB_Name}"
Postgres_User="${Service_DB_User}"
Postgres_Password="${Service_DB_Password}"
