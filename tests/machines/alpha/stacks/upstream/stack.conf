# A proxy to a container this machine does not manage. Exercises the "upstreams
# of the enabled vhosts" block: it must notice that the target is not alive.
Requires=""
Domains="api.example.com"
Containers="no"
